pre-commit:
  parallel: true
  commands:
    fmt:
      run: golangci-lint fmt ./...
    lint:
      run: golangci-lint run ./...
    build:
      run: go build ./...

pre-push:
  commands:
    test:
      run: go test -race -cover ./...
